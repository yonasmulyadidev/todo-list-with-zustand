import { CSSProperties } from 'react';

const todoCardStyles: CSSProperties = {
  backgroundColor: '#e3e3e3',
  borderRadius: '5px',
};

const todoButtonStyles: CSSProperties = {
  textTransform: 'none',
};

export { todoButtonStyles, todoCardStyles };
